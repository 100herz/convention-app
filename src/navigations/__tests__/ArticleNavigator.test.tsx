import React from 'react'
import { render } from '@testing-library/react-native'

import MainNavigation from '../ArticleNavigator'

describe('<Button />', () => {
  // TODO: Add the tests
  it('renders correctly', () => {
    //   const { toJSON } = render(<MainNavigation />)
    //   expect(toJSON()).toMatchSnapshot()
  })
})
